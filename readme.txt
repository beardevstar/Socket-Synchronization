First, run server : $> ./server
Second, run client : $> ./client 127.0.0.7 8888

then client read inputfile.txt file and send words to server,
server change it with 2 processes and 7 threads with Queues.

after finished, the result.txt file is added.
